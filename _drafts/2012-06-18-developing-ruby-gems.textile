---
layout: post
title: Developing Ruby Gems
enki_id: 8
categories: ruby, gems, keycutter
---
h2. Creating a gem from scratch

# Create a skeleton gem
@$ bundle gem my_awesome_gem@
# Write code and tests
# Build and install gem locally
@$ rake install@ 
Or
@$ rake build@
@$ gem install pkg/my_awesome_gem-0.0.4.gem@
# (optional) Push the gem to rubygems.org
@$ gem push pkg/my_awesome_gem-0.0.4.gem@

h2. Updating an existing gem

# Make the code changes
# Update the version number in @version.rb@
# Build and install the gem locally
@$ rake install@
# (optional) Push the gem to rubygems.org
@$ gem push pkg/my_awesome_gem-0.0.4.gem@

h2. Unpacking a gem into a ruby application

@$ gem unpack my_awesome_gem -v 0.0.4 --target vendor/gems@

h2. Pushing gems to rubygems.org

h3. Pushing a new version

@$ gem push pkg/my_awesome_gem-0.0.4.gem@

h3. Dealing with multiple rubygems accounts

# Install "keycutter":https://github.com/joshfrench/keycutter
# View existing keys (default key is marked with a *) 
@$ gem keys --list@
# Add a new key (you'll be prompted for your credentials) 
@$ gem keys --add work@
# Change the key used to connect to rubygems 
@$gem keys --default work@
